# Keep the classic node_modules layout: Jest 26 and Next 12 predate Plug'n'Play,
# which is what Yarn would use by default.
nodeLinker: node-modules

# The release is committed so any Yarn on PATH delegates to this exact version,
# with or without Corepack.
yarnPath: .yarn/releases/yarn-4.18.0.cjs
